package oskgunnlaugs;

import java.util.List;

import ng.appserver.NGComponent;
import ng.appserver.NGContext;

public class Wrapper extends NGComponent {

	public Wrapper(NGContext context) {
		super(context);
	}
}